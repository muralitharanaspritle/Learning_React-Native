"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _default = (today, from, to) => {
  let timeToday = today.getTime();

  if (from && to) {
    return timeToday >= new Date(from).getTime() && timeToday <= new Date(to).getTime();
  }

  if (from) {
    return timeToday >= new Date(from).getTime();
  }

  if (to) {
    return timeToday <= new Date(to).getTime();
  }

  return false;
};

exports.default = _default;
//# sourceMappingURL=dateInRange.js.map